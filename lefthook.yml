pre-commit:
  commands:
    biome-check:
      glob: "*.{js,jsx,ts,tsx,astro,json,jsonc,css}"
      run: pnpm -s biome:fix {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
